# Core Dependencies
psycopg2-binary>=2.9.9
anthropic>=0.40.0

# MCP SDK (optional for standalone use)
mcp>=0.9.0

# Optional: Better type hints and validation
pydantic>=2.0.0

# Development & Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Optional: SQL parsing for better column extraction
sqlparse>=0.5.0
